<UserControl x:Class="CulinaryAssistant.UI.Views.ShoppingListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converters="clr-namespace:CulinaryAssistant.UI.Converters"
             mc:Ignorable="d">
    
    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
    </UserControl.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!-- Список списков покупок -->
        <Border Grid.Column="0" 
                Background="{DynamicResource CardBrush}" 
                CornerRadius="12" 
                Margin="0,0,16,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- Заголовок -->
                <Border Grid.Row="0" Padding="16" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1">
                    <Grid>
                        <TextBlock Text="Списки покупок" 
                                   FontSize="16" FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextBrush}"
                                   VerticalAlignment="Center"/>
                        <Button Content="+" 
                                Command="{Binding CreateListCommand}"
                                HorizontalAlignment="Right"
                                Style="{DynamicResource IconButtonStyle}"/>
                    </Grid>
                </Border>
                
                <!-- Списки -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding ShoppingLists}" Margin="8">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{DynamicResource BackgroundBrush}" 
                                        CornerRadius="8" 
                                        Padding="12" 
                                        Margin="0,0,0,8"
                                        Cursor="Hand">
                                    <Border.InputBindings>
                                        <MouseBinding MouseAction="LeftClick" 
                                                      Command="{Binding DataContext.SelectListCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                      CommandParameter="{Binding}"/>
                                    </Border.InputBindings>
                                    
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        
                                        <StackPanel Grid.Row="0" Orientation="Horizontal">
                                            <TextBlock Text="{Binding Name}" 
                                                       FontWeight="Medium"
                                                       Foreground="{DynamicResource TextBrush}"/>
                                            <Border CornerRadius="4" Padding="6,2" Margin="8,0,0,0"
                                                    Visibility="{Binding IsCompleted, Converter={StaticResource BoolToVisibilityConverter}}">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Background" Value="{DynamicResource SuccessBrush}"/>
                                                    </Style>
                                                </Border.Style>
                                                <TextBlock Text="✓" FontSize="10" Foreground="White"/>
                                            </Border>
                                        </StackPanel>
                                        
                                        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,6,0,0">
                                            <TextBlock Text="{Binding PurchasedItems}" FontSize="12" 
                                                       Foreground="{DynamicResource MutedTextBrush}"/>
                                            <TextBlock Text="/" FontSize="12" 
                                                       Foreground="{DynamicResource MutedTextBrush}"/>
                                            <TextBlock Text="{Binding TotalItems}" FontSize="12" 
                                                       Foreground="{DynamicResource MutedTextBrush}"/>
                                            <TextBlock Text=" куплено" FontSize="12" 
                                                       Foreground="{DynamicResource MutedTextBrush}"/>
                                        </StackPanel>
                                        
                                        <!-- Прогресс бар -->
                                        <Border Grid.Row="2" Height="4" CornerRadius="2" 
                                                Background="{DynamicResource BorderBrush}" 
                                                Margin="0,8,0,0">
                                            <Border CornerRadius="2" 
                                                    Background="{DynamicResource PrimaryBrush}"
                                                    HorizontalAlignment="Left">
                                                <Border.Width>
                                                    <MultiBinding Converter="{StaticResource ProgressToWidthConverter}">
                                                        <Binding Path="CompletionPercentage"/>
                                                        <Binding Path="ActualWidth" RelativeSource="{RelativeSource AncestorType=Border}"/>
                                                    </MultiBinding>
                                                </Border.Width>
                                            </Border>
                                        </Border>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>
        
        <!-- Детали выбранного списка -->
        <Border Grid.Column="1"
                Background="{DynamicResource CardBrush}"
                CornerRadius="12">
            <Grid>
                <!-- Нет выбранного списка -->
                <TextBlock Text="Выберите список покупок"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           FontSize="16"
                           Foreground="{DynamicResource MutedTextBrush}"
                           Visibility="{Binding HasSelectedList, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>

                <!-- Выбранный список -->
                <Grid Visibility="{Binding HasSelectedList, Converter={StaticResource BoolToVisibilityConverter}}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Заголовок списка -->
                <Border Grid.Row="0" Padding="20" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="{Binding SelectedList.Name}" 
                                       FontSize="20" FontWeight="SemiBold"
                                       Foreground="{DynamicResource TextBrush}"/>
                            <TextBlock FontSize="13" Margin="0,4,0,0"
                                       Foreground="{DynamicResource MutedTextBrush}">
                                <Run Text="{Binding SelectedList.CompletionPercentage, StringFormat='{}{0:F0}%'}"/>
                                <Run Text=" выполнено"/>
                            </TextBlock>
                        </StackPanel>
                        
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <Button Content="📤 CSV" 
                                    Command="{Binding ExportListCommand}"
                                    Style="{DynamicResource SecondaryButtonStyle}"
                                    Margin="0,0,8,0"/>
                            <Button Content="{Binding CompleteButtonText}" 
                                    Command="{Binding ToggleCompleteCommand}"
                                    Style="{DynamicResource SuccessButtonStyle}"
                                    Margin="0,0,8,0"/>
                            <Button Content="🗑️" 
                                    ToolTip="Удалить список"
                                    Command="{Binding DeleteListCommand}"
                                    Style="{DynamicResource IconButtonStyle}"/>
                        </StackPanel>
                    </Grid>
                </Border>
                
                <!-- Элементы списка -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="20,16">
                    <ItemsControl ItemsSource="{Binding SelectedList.Items}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="16" Margin="0,0,0,8" 
                                        Background="{DynamicResource BackgroundBrush}"
                                        CornerRadius="8">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <CheckBox Grid.Column="0"
                                                  IsChecked="{Binding IsPurchased, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                  VerticalAlignment="Center"
                                                  Margin="0,0,12,0"/>
                                        
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Name}" 
                                                       FontWeight="Medium"
                                                       Foreground="{DynamicResource TextBrush}">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsPurchased}" Value="True">
                                                                <Setter Property="TextDecorations" Value="Strikethrough"/>
                                                                <Setter Property="Opacity" Value="0.5"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock FontSize="12" Foreground="{DynamicResource MutedTextBrush}">
                                                    <Run Text="{Binding Quantity}"/>
                                                    <Run Text=" "/>
                                                    <Run Text="{Binding Unit}"/>
                                                </TextBlock>
                                                <TextBlock Text=" · " FontSize="12" 
                                                           Foreground="{DynamicResource MutedTextBrush}"
                                                           Visibility="{Binding HasStore, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                                <TextBlock Text="{Binding PreferredStore}" FontSize="12" 
                                                           Foreground="{DynamicResource MutedTextBrush}"
                                                           Visibility="{Binding HasStore, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                            </StackPanel>
                                        </StackPanel>
                                        
                                        <TextBlock Grid.Column="2" 
                                                   Text="{Binding EstimatedPrice, StringFormat='€ {0:F2}'}"
                                                   VerticalAlignment="Center"
                                                   Foreground="{DynamicResource MutedTextBrush}"
                                                   Margin="0,0,16,0"
                                                   Visibility="{Binding HasPrice, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                        
                                        <Button Grid.Column="3" Content="×" 
                                                Command="{Binding DataContext.RemoveItemCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding}"
                                                Style="{DynamicResource IconButtonStyle}"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
                
                <!-- Добавление нового элемента -->
                <Border Grid.Row="2" Padding="20" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBox Grid.Column="0" 
                                 Text="{Binding NewItemName, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{DynamicResource TextBoxStyle}"
                                 Margin="0,0,8,0"/>
                        
                        <TextBox Grid.Column="1" 
                                 Text="{Binding NewItemQuantity, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{DynamicResource TextBoxStyle}"
                                 Margin="0,0,8,0"/>
                        
                        <Button Grid.Column="2" 
                                Content="Добавить" 
                                Command="{Binding AddItemCommand}"
                                Style="{DynamicResource PrimaryButtonStyle}"/>
                    </Grid>
                </Border>
            </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
