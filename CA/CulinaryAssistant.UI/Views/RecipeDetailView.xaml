<UserControl x:Class="CulinaryAssistant.UI.Views.RecipeDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converters="clr-namespace:CulinaryAssistant.UI.Converters"
             mc:Ignorable="d">
    
    <UserControl.Resources>
        <converters:StatusToColorConverter x:Key="StatusToColorConverter"/>
        <converters:EnumToStringConverter x:Key="EnumToStringConverter"/>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Заголовок с кнопками -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <Button Grid.Column="0" 
                    Content="← Назад" 
                    Command="{Binding GoBackCommand}"
                    Style="{DynamicResource SecondaryButtonStyle}"
                    Margin="0,0,16,0"/>
            
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="{Binding Recipe.Name, FallbackValue='Новый рецепт'}" 
                           FontSize="24" FontWeight="Bold" 
                           Foreground="{DynamicResource TextBrush}"/>
                <Border Background="{Binding Recipe.Status, Converter={StaticResource StatusToColorConverter}}"
                        CornerRadius="4" Padding="10,4" Margin="16,0,0,0"
                        VerticalAlignment="Center">
                    <TextBlock Text="{Binding Recipe.Status, Converter={StaticResource EnumToStringConverter}}" 
                               FontSize="12" Foreground="White"/>
                </Border>
            </StackPanel>
            
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <!-- Кнопки workflow -->
                <Button Content="Опубликовать" 
                        Command="{Binding PublishCommand}"
                        Visibility="{Binding CanPublish, Converter={StaticResource BoolToVisibilityConverter}}"
                        Style="{DynamicResource SuccessButtonStyle}"
                        Margin="0,0,8,0"/>
                <Button Content="В архив" 
                        Command="{Binding ArchiveCommand}"
                        Visibility="{Binding CanArchive, Converter={StaticResource BoolToVisibilityConverter}}"
                        Style="{DynamicResource WarningButtonStyle}"
                        Margin="0,0,8,0"/>
                <Button Content="Восстановить" 
                        Command="{Binding RestoreCommand}"
                        Visibility="{Binding CanRestore, Converter={StaticResource BoolToVisibilityConverter}}"
                        Style="{DynamicResource SecondaryButtonStyle}"
                        Margin="0,0,8,0"/>
                <Button Content="В черновик" 
                        Command="{Binding ReturnToDraftCommand}"
                        Visibility="{Binding CanReturnToDraft, Converter={StaticResource BoolToVisibilityConverter}}"
                        Style="{DynamicResource SecondaryButtonStyle}"
                        Margin="0,0,8,0"/>
                <Button Content="💾 Сохранить" 
                        Command="{Binding SaveCommand}"
                        Visibility="{Binding IsEditing, Converter={StaticResource BoolToVisibilityConverter}}"
                        Style="{DynamicResource PrimaryButtonStyle}"/>
            </StackPanel>
        </Grid>
        
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="350"/>
                </Grid.ColumnDefinitions>
                
                <!-- Основная информация -->
                <StackPanel Grid.Column="0" Margin="0,0,24,0">
                    <!-- Основные поля -->
                    <Border Background="{DynamicResource CardBrush}" 
                            CornerRadius="12" Padding="24" Margin="0,0,0,16">
                        <StackPanel>
                            <TextBlock Text="Основная информация" 
                                       FontSize="16" FontWeight="SemiBold" 
                                       Foreground="{DynamicResource TextBrush}"
                                       Margin="0,0,0,16"/>
                            
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                
                                <!-- Код -->
                                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,12,16">
                                    <TextBlock Text="Код рецепта" Style="{DynamicResource LabelStyle}"/>
                                    <TextBox Text="{Binding Recipe.Code, UpdateSourceTrigger=PropertyChanged}"
                                             IsEnabled="{Binding IsNewRecipe}"
                                             Style="{DynamicResource TextBoxStyle}"/>
                                </StackPanel>
                                
                                <!-- Название -->
                                <StackPanel Grid.Row="0" Grid.Column="1" Margin="12,0,0,16">
                                    <TextBlock Text="Название" Style="{DynamicResource LabelStyle}"/>
                                    <TextBox Text="{Binding Recipe.Name, UpdateSourceTrigger=PropertyChanged}"
                                             IsEnabled="{Binding CanEdit}"
                                             Style="{DynamicResource TextBoxStyle}"/>
                                </StackPanel>
                                
                                <!-- Кухня -->
                                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,12,16">
                                    <TextBlock Text="Кухня" Style="{DynamicResource LabelStyle}"/>
                                    <TextBox Text="{Binding Recipe.Cuisine, UpdateSourceTrigger=PropertyChanged}"
                                             IsEnabled="{Binding CanEdit}"
                                             Style="{DynamicResource TextBoxStyle}"/>
                                </StackPanel>
                                
                                <!-- Тип блюда -->
                                <StackPanel Grid.Row="1" Grid.Column="1" Margin="12,0,0,16">
                                    <TextBlock Text="Тип блюда" Style="{DynamicResource LabelStyle}"/>
                                    <ComboBox ItemsSource="{Binding DishTypes}"
                                              SelectedItem="{Binding Recipe.DishType}"
                                              IsEnabled="{Binding CanEdit}"
                                              Style="{DynamicResource ComboBoxStyle}"/>
                                </StackPanel>
                                
                                <!-- Время приготовления -->
                                <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,12,0">
                                    <TextBlock Text="Время (мин)" Style="{DynamicResource LabelStyle}"/>
                                    <TextBox Text="{Binding Recipe.CookingTimeMinutes, UpdateSourceTrigger=PropertyChanged}"
                                             IsEnabled="{Binding CanEdit}"
                                             Style="{DynamicResource TextBoxStyle}"/>
                                </StackPanel>
                                
                                <!-- Порции -->
                                <StackPanel Grid.Row="2" Grid.Column="1" Margin="12,0,0,0">
                                    <TextBlock Text="Порций" Style="{DynamicResource LabelStyle}"/>
                                    <TextBox Text="{Binding Recipe.Servings, UpdateSourceTrigger=PropertyChanged}"
                                             IsEnabled="{Binding CanEdit}"
                                             Style="{DynamicResource TextBoxStyle}"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>
                    
                    <!-- Описание -->
                    <Border Background="{DynamicResource CardBrush}" 
                            CornerRadius="12" Padding="24" Margin="0,0,0,16">
                        <StackPanel>
                            <TextBlock Text="Описание" 
                                       FontSize="16" FontWeight="SemiBold" 
                                       Foreground="{DynamicResource TextBrush}"
                                       Margin="0,0,0,12"/>
                            <TextBox Text="{Binding Recipe.Description, UpdateSourceTrigger=PropertyChanged}"
                                     IsEnabled="{Binding CanEdit}"
                                     TextWrapping="Wrap"
                                     AcceptsReturn="True"
                                     MinHeight="80"
                                     Style="{DynamicResource TextBoxStyle}"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- Инструкции -->
                    <Border Background="{DynamicResource CardBrush}" 
                            CornerRadius="12" Padding="24">
                        <StackPanel>
                            <TextBlock Text="Инструкции по приготовлению" 
                                       FontSize="16" FontWeight="SemiBold" 
                                       Foreground="{DynamicResource TextBrush}"
                                       Margin="0,0,0,12"/>
                            <TextBox Text="{Binding Recipe.Instructions, UpdateSourceTrigger=PropertyChanged}"
                                     IsEnabled="{Binding CanEdit}"
                                     TextWrapping="Wrap"
                                     AcceptsReturn="True"
                                     MinHeight="200"
                                     Style="{DynamicResource TextBoxStyle}"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
                
                <!-- Боковая панель - ингредиенты и категории -->
                <StackPanel Grid.Column="1">
                    <!-- Ингредиенты -->
                    <Border Background="{DynamicResource CardBrush}" 
                            CornerRadius="12" Padding="20" Margin="0,0,0,16">
                        <StackPanel>
                            <Grid Margin="0,0,0,12">
                                <TextBlock Text="Ингредиенты" 
                                           FontSize="16" FontWeight="SemiBold" 
                                           Foreground="{DynamicResource TextBrush}"/>
                                <Button Content="+" 
                                        Command="{Binding AddIngredientCommand}"
                                        Visibility="{Binding CanEdit, Converter={StaticResource BoolToVisibilityConverter}}"
                                        HorizontalAlignment="Right"
                                        Style="{DynamicResource IconButtonStyle}"/>
                            </Grid>
                            
                            <ItemsControl ItemsSource="{Binding Recipe.Ingredients}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="{DynamicResource BackgroundBrush}" 
                                                CornerRadius="8" Padding="12" Margin="0,0,0,8">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                
                                                <StackPanel Grid.Column="0">
                                                    <TextBlock Text="{Binding Name}" FontWeight="Medium"
                                                               Foreground="{DynamicResource TextBrush}"/>
                                                    <TextBlock FontSize="12" Foreground="{DynamicResource MutedTextBrush}">
                                                        <Run Text="{Binding Amount}"/>
                                                        <Run Text=" "/>
                                                        <Run Text="{Binding Unit, Converter={StaticResource EnumToStringConverter}}"/>
                                                    </TextBlock>
                                                </StackPanel>
                                                
                                                <Button Grid.Column="1" Content="×" 
                                                        Command="{Binding DataContext.RemoveIngredientCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                        CommandParameter="{Binding}"
                                                        Visibility="{Binding DataContext.CanEdit, RelativeSource={RelativeSource AncestorType=ItemsControl}, Converter={StaticResource BoolToVisibilityConverter}}"
                                                        Style="{DynamicResource IconButtonStyle}"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            
                            <TextBlock Text="Нет ингредиентов" 
                                       FontSize="13" 
                                       Foreground="{DynamicResource MutedTextBrush}"
                                       HorizontalAlignment="Center"
                                       Visibility="{Binding HasNoIngredients, Converter={StaticResource BoolToVisibilityConverter}}"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- Категории -->
                    <Border Background="{DynamicResource CardBrush}" 
                            CornerRadius="12" Padding="20">
                        <StackPanel>
                            <TextBlock Text="Категории" 
                                       FontSize="16" FontWeight="SemiBold" 
                                       Foreground="{DynamicResource TextBrush}"
                                       Margin="0,0,0,12"/>
                            
                            <ItemsControl ItemsSource="{Binding AllCategories}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <CheckBox Content="{Binding Name}"
                                                  IsChecked="{Binding IsSelected}"
                                                  IsEnabled="{Binding DataContext.CanEdit, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                  Margin="0,0,12,8"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
